# Dependencias do app IA Local (instaladas automaticamente pelo start.bat).
#
# llama-cpp-python: o start.bat instala ANTES a build acelerada certa para o seu
# hardware (CUDA / Vulkan / CPU), vinda de https://abetlen.github.io/llama-cpp-python/whl/<backend>.
# Com ela instalada, esta linha so confirma a versao minima: a 0.3.26+ traz o
# handler multimodal generico (libmtmd) usado para os modelos de visao (VLM).
llama-cpp-python>=0.3.26,<0.4
numpy>=1.24
jinja2>=3.1
diskcache>=5.6
typing-extensions>=4.5

# Interface web
gradio>=6.28,<7

# Busca web autonoma da IA (unica API de busca usada)
tavily-python>=0.7,<1

# Leitura nativa de arquivos
pypdf>=5.0
python-docx>=1.1
openpyxl>=3.1
python-pptx>=1.0
pillow>=10.0
charset-normalizer>=3.3

# Utilidades
python-dotenv>=1.0

# Dependencias do app IA Local (instaladas automaticamente pelo start.bat).
#
# llama-cpp-python: o start.bat instala ANTES a build certa para o seu hardware
# (wheel CUDA, ou wheel CPU + plugin oficial de GPU Vulkan/CUDA do llama.cpp).
# A versao e fixada porque os plugins oficiais de GPU (releases b10442-b10453 do
# llama.cpp) tem a pasta ggml/ identica a desta versao: ABI identica.
llama-cpp-python==0.3.35
numpy>=1.24
jinja2>=3.1
diskcache>=5.6
typing-extensions>=4.5

# Interface web
gradio>=6.28,<7

# Busca web autonoma da IA (unica API de busca usada)
tavily-python>=0.7,<1

# Leitura nativa de arquivos
pypdf>=5.0
python-docx>=1.1
openpyxl>=3.1
python-pptx>=1.0
pillow>=10.0
charset-normalizer>=3.3

# Utilidades
python-dotenv>=1.0
